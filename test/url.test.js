import _url from '../utils/url';
import { assert } from 'assert';

describe('url', () => {
  describe('getPath', () => {
    it('should return whole path when no param', () => {
      
    });
  });
});
